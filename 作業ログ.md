# かめペース 作業ログ（指示・理由・対応）

このファイルは、開発の指示内容と（言われていれば）その理由、対応した変更を記録するものです。
**続きを作る人（他の人・未来の自分）は、まずこのファイルと `design_handoff_kamepace/CLAUDE.md`（恒久ルール）を読んでください。**
新しい作業をしたら、このファイルの「作業ログ」に追記していきます（新しいものを上に）。

---

## プロジェクト概要
- **かめペース**: 疲労を行動記録から推定し、絵文字を積んで可視化する Web アプリ（がんばりすぎ防止）。
- **本体**: `kamepace/`（Vite + React）。物理演算は Matter.js（DOMの`<div>`をtransformで動かす方式・canvasは使わない）。
- **本番**: Vercel。`main` に push すると本番デプロイ。作業は `feature/bookshelf` ブランチ。
- **ブランチ運用**: 実装→ブラウザ実測→`feature/bookshelf`にコミット→`main`に`--no-ff`マージ→push、まで毎回自動で行う（特に実機で試す系）。マージ後は`feature/bookshelf`に戻る。
- **データ連携**: Firebase（カレンダーアプリ `mylifecore`＝`anitigravity/my-schedule-app` と同一プロジェクト）。ログインすると予定・タスクを自動取り込み。かめデータは `users/{uid}/apps/kamepace`。
- **ゲスト**: 未ログインは標準でデザイン学部2年ペルソナの1学年サンプルを表示（`kamepace/src/sample.js`）。

## 守るべきルール（既知のこだわり）
- **絵文字の山サイズ**: `calcR()` の `FILL_UNIT=125`（この個数で画面いっぱい）。大きすぎ/小さすぎ両方NG。100には戻さない（大きすぎと言われた経緯）。
- **UIに冗長な文を足さない**: 説明キャプション・注意書き・色付きの囲み枠を勝手に付けない。ラベルと操作要素だけで簡潔に。
- **シャカ効果音**: iOS=HTMLAudio方式（iOSのWeb Audioは砂ノイズになる）、Android等=Web Audio。安易にWeb Audio統一しない。
- **極力質問せず**自律的に実装→検証→push→マージまで完遂。合理的なデフォルトで進め、違ったら直す。

---

## 作業ログ（新しい順）

### 2026-08-26 記録に日付を指定できるように（ストップウォッチ的記録の当日固定を解消）
- **指示**: ストップウォッチで記録して登録すると必ず当日になる。22日の12:00〜16:00を記録し忘れて翌23日に記録すると23日になる。時間だけでなく日付も紐づけて。
- **原因/理由**: 記録日が `homeDateStr()`(当日)固定だった。取り込みタスクのチェック記録(`toggleHomeTask`)も `todayStr()` 固定。
- **対応**: `state.recordDate` 追加。時刻モードの確認画面に「日付」欄（デフォルト当日・変更可）。`commitSearch` はこの日付で記録。過去日は「きろくする」（`commitLabel`を日付考慮）。`toggleHomeTask` はタスクの日付 `t.date` で記録。時刻モードの冗長な説明文も削除。

### 2026-08-21 時刻入力が勝手に戻る不具合を修正 `f856c5a`
- **指示**: 行動の時間を設定するとき勝手に時間が書き換えられる（13:00〜14:00にしたのに21:18〜21:47に戻る／入力しても別時刻で記録される）。
- **原因/理由**: 時刻モードで行動に`ranges`未設定のとき、表示はstartTime基準のフォールバック(現在時刻)を出すのに、`setRange`が空配列を触って入力を捨てていた。
- **対応**: `setRange`で表示と同じ初期レンジを土台にしてから編集を適用。

### 2026-07-31 記録した絵文字が1個ずつしか降らない `489dd2e`
- **指示**: 行動追加してシャカ画面に移っても1個くらいしか降ってこない。往復すると徐々に全部出る。全部いっぺんに降らせて。
- **原因/理由**: 新規分の落下開始位置が画面の最大0.6倍も上に散らばり遠くから降っていた＋記録直後の`startPhysics`がタイミングで確実に走らないことがあった。
- **対応**: 新規を画面上端すぐ上に段積み配置。`startPhysics`をrAF二重＋setTimeout(160)フォールバックで確実に1回起動。

### 2026-07-26 ジャイロで大きい回復が数個しか消えない `c39c47c`
- **指示**: 仮眠で-96のはずが4個しか降らない（ジャイロ使用）。振る時はセンサー気にしなくていい。
- **原因/理由**: 回復の🌙投下を遅延させた結果、箱を組む時点で「回復中」フラグ未設定→ジャイロの閉じた箱で組み、🌙が蓋に阻まれた。
- **対応**: `commitSearch`で`startPhysics`前に`_recoverUntil`を立て、回復中は箱を開け・重力真下・センサー無視。

### 2026-07-25 回復記録が1個しか消費されない `7b03d26`
- **原因/理由**: 記録直後は山を積み直すため、落ち着いていない山を🌙がすり抜けていた。
- **対応**: 🌙の投下を山が落ち着く約0.85秒後に遅延。

### 2026-07-25 記録する行動を最低±1に `85441b7`
- **指示**: 四捨五入で0になっても最低±1にして。
- **対応**: `effFat(item,min)`ヘルパー追加。記録・表示・取り込みテンプレに適用。

### 2026-07-25 ログイン山ぎっちぎち／記録後に降らない／ジャイロふわふわ `4f316eb`
- **指示**: ログアウト→再ログインでその日のシャカがぎっちぎち（再起動で戻る）。ジャイロの重力を強く（振れるくらい）。
- **原因/理由**: ログイン時に前(ゲスト)の物理エンジンが残っていた。
- **対応**: `loadCloud`/`loadGuest`で`stopPhysics`して組み直し。記録後の物理起動を`setState`コールバックに移動。ジャイロ重力 1.4→3.2。

### 2026-07-24 睡眠/回復のconsumed累積バグ `3de411d` / ジャイロ時 `16d9208`
- **指示**: 睡眠記録でラインをどこにしても2〜4個しか回復しない。加速度時はOK・ジャイロ時がダメ→ジャイロが原因では。睡眠中はセンサー切って。
- **原因/理由**: (1)`checkNegativeCollisions`が`this.state`を直接読み非同期バッチで消費が累積しなかった。(2)ジャイロは閉じた箱＋傾き重力で🌙が山に届かなかった。
- **対応**: 関数型`setState`で累積。回復落下中は箱を開け・重力真下・センサー無視。

### 2026-07-23 mylifecoreタスク取り込み `21996ef` `ce22fa7`
- **指示**: mylifecoreの日付に紐づいたタスクをタスク欄に表示して。毎日ページのタスクが取り込まれていない。
- **原因/理由**: プロジェクトタスクは今日ぶんのみ・日付なしだった。毎日ページのタスクは`taskList`フィールドにあるのに`.tasks`しか読んでいなかった。
- **対応**: プロジェクトタスクを今日〜+7日・日付付きで取得。`taskList`と`tasks`両方読む。完了書き戻しも両対応。

### 2026-07-23 取り込み予定の全体時刻を保持 `ff208e1` `6c59be9`
- **指示**: Google/mylifecoreの予定は勝手に時刻を変えないで。確認画面に予定の全体時間(例9:00-10:30)を常時表示。所要時間モードはその中で分配、時刻モードは行動ごとの時刻に全体を合わせない。手動変更は可。緑枠と説明文はいらない（冗長）。
- **対応**: 確認画面に「予定の全体の時間」行（ラベル＋時刻入力のみ）。枠がある時は全体時刻を自動変更しない。

### 2026-07-23 ジャイロ切替・調整 `b4d5a3b` `6c59be9` `6293b6e` `e01f750`
- **指示**: シャカを加速度センサー（振る）とジャイロ（傾き＝逆さで上辺に絵文字が集まる）でマイページから切替。重力大きく。左右が逆。
- **対応**: マイページに「動かし方（振る/傾き）」トグル。ジャイロは箱を上辺で閉じる。x符号は iOS=+ag.x/Android=-ag.x（左に傾けたら左）。

### 2026-07-23 行動・カテゴリの管理 `52102d2`
- **指示**: ユーザーが作った行動を消す/非表示にできるように。大カテゴリ作成時にカテゴリ名の行動を勝手に作らないで。似た行動を他カテゴリからも参照できるように。
- **対応**: カテゴリ管理で行動を非表示/表示・自作は削除（`hiddenActs`永続化）。新カテゴリは空で作る。コピー元を全カテゴリから選べる。

### 2026-07-22 iOSパフォーマンス・音 `c481e4f`〜`773925a`
- **指示**: iPhoneで動作が重い。音は元のsyakasyaka.mp3がいい。iOSは砂みたいな音になる／無音になる。
- **原因/理由**: 重さの主因は物理（可変デルタ・solver反復多）と描画（drop-shadowのDPR再ラスタライズ）。iOSのWeb Audioはこの音を砂ノイズ化する。
- **対応**: 物理をfixed timestep化＋反復削減（→さらに8/4へ）。動く絵文字のdrop-shadow除去。音はiOS=HTMLAudio/Android=Web Audioに分岐。

### 2026-07-22 まとめ機能追加 `c481e4f`
- **指示**: シャカ絵文字を小さく。mylifecore枠は枠の時刻を使う。時間設定に+1時間ステッパー。初回ゲスト/ログインにチュートリアル復活。サンプルの積もりすぎ解消。取り込みデータ削除。タスク手動追加＋行動紐づけ。シャカを全期間表示。未来の予定は灰色で積む。
- **対応**: `FILL_UNIT`100→125。サンプルのバイト疲労と繰り越しを縮小。各機能を実装。

---

## 注意点・未解決
- iOSのHTMLAudio音は根本的に`play()`が重い。まだ重ければクールダウンを更に伸ばす等で対応余地あり。
- ジャイロの符号は実機（iOS/Android）で最終確認が要る。逆なら符号を反転。
- 背景タブでは`requestAnimationFrame`が止まるため、物理のE2E検証は手動`Engine.update`で行う（実機と挙動が異なる点に注意）。
